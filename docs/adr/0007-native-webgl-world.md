---
status: accepted
---

# Render the Archive as a native WebGL world

The explorable Archive will be a genuine 3D environment rendered with native WebGL rather than a column raycaster or a sequence of illustrated scenes. Local textures, geometry, lighting, fog, particles, doors, and animated objects will provide the high-resolution world while retaining direct-file operation and avoiding an engine, package manager, external runtime, or network dependency.
